# 战略配置基准示例（ADR-0001/0003）。
# 复制为 config/baseline.yaml 后填入你自己裁定的区间（P0 只提供配置位，
# 数字在看到真实穿透暴露后由你决定）。键为穿透后暴露分类：CN/HK/US/CASH/BOND。
# 删除或留空即回到「未设定」状态，首页改为展示真实暴露并引导定基准。
CN:   {min: 0.25, max: 0.45}
HK:   {min: 0.05, max: 0.20}
US:   {min: 0.20, max: 0.45}
CASH: {min: 0.05, max: 0.20}
